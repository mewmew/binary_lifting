% related work, state of the art.

% === [ Modelling the CPU ] ====================================================

\section{Modelling the CPU}

CPU and FPU state modelling

* Global CPU state structure passed as pointer between functions (MC-Semantrics).

* Emulator of the CPU, compiled down to LLVM IR (Fcd).

% TODO: add note about learning the semantics of compilers.
% ref: 2016 lifting to IR using compilers
% > Learning from compilers. C -> IR -> asm (learn) -> IR
